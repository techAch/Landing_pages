# قالب صفحة هبوط لبيع الأحذية

**ملف المشروع:** `قالب_صفحة_هبوط_لبيع_الأحذية.html`

## 📌 وصف المشروع
بسيط وسريع: صفحة هبوط باللغة العربية لعرض منتج (حذاء) مع عداد تنازلي، خيارات منتج (ألوان، مقاسات)، نموذج طلب يرسل البيانات إلى Google Apps Script، وأزرار تواصل (واتساب) وروابط التواصل الاجتماعي.

## ✨ الميزات الرئيسية
- **Hero** مع عنوان مركزي وشرح قصير (`.hero-title`, `.hero-subtitle`).
- **عداد تنازلي** يستمر بدورات (4 ساعات) ويتم حفظ النهاية في `localStorage` (مفتاح: `adCycleEnd_<PRODUCT_CODE>`).
- **معاينة صور** وقسم صور مصغرة يدعم تبديل الصورة (`swapImage`).
- **خيارات المنتج**: ألوان قابلة للاختيار (`.color`) ومقاسات (`#size`).
- **نموذج الطلب** مع تحقق عميل (validation) وعرض رسائل خطأ مخفية بشكل افتراضي (`.error-msg`).
- **إرسال الطلب** بصيغة `application/x-www-form-urlencoded` (URLSearchParams) إلى `AppScriptURL` لتفادي طلب OPTIONS/CORS مع Google Apps Script.
- **تحسينات وصولية**: تسميات `label`, `aria-disabled` للأزرار المعطّلة، وفعل `focusAndScrollTo` للتركيز على الحقول الناقصة.

## 🧭 كيف تختبر وتشغل محليًا
- أبسط طريقة: افتح `قالب_صفحة_هبوط_لبيع_الأحذية.html` في المتصفح مباشرة (نقرة مزدوجة). لكن ملاحظة مهمة حول الشبكات: إذا أردت اختبار الإرسال عبر `fetch` إلى `AppScriptURL` فجرب عبر سيرفر محلي HTTP (مثلاً `python -m http.server 8000`) لأن بعض المتصفحات تقيد بعض أنواع الشبكات عند استخدام `file://`.
- اختبر شاشات الجوال باستخدام DevTools (عرض 320px، 375px، 428px) للتأكد من عدم انكسار العنوان أو التفافه.

## ⚙️ إعداد ونشر Google Apps Script
- في الملف يوجد ثابت: `AppScriptURL` (استبدله برابط الـ **Web App** الخاص بك — رابط /exec).
- عند نشر السكربت: اختر نشر كـ **Web App** وامنح الوصول لـ **Anyone, even anonymous** (أو اضبط سياسات الاعتماد حسب حاجتك).
- نصيحة: الـ client يرسل `application/x-www-form-urlencoded` لتجنب OPTIONS preflight مع Apps Script.

## 🐞 المشكلات المعروفة / نصائح للتصحيح
- إذا ظهر خطأ **401 أو CORS** عند الإرسال: تأكد من نشر سكربت Google Apps Script كـ `/exec` وأن الوصول مضبوط، ثم اختبر الـ response headers في Console (Network).
- إذا اختفى زر الإرسال بعد التفاعل مع الحقول: تأكد من وجود أحدث نسخ السكربت — تم إصلاح سبب اختفاءه (مشكلة في افتراض `nextElementSibling`).
- العنوان الرئيسي `.hero-title` مركز عبر العرض وتمت إضافة مساحات عليا (`padding-top`) ولكن راجع العرض على أحجام صغيرة (320–375–428) وقد تحتاج لتقليل `top` أو `padding-top` إذا بدا قريبًا جدًا من الحافة.

## 🔧 متغيرات وسلوكيات مهمة في الكود
- `PRODUCT_CODE` — رمز المنتج المستخدم لتسمية المفاتيح في `sessionStorage`/`localStorage`.
- `PRODUCT_OPTIONS` — يحدد ما إذا كانت ألوان/مقاسات موجودة.
- `COUNTDOWN_KEY` — مفتاح العداد في `localStorage` (يستخدم `PRODUCT_CODE`).
- `AppScriptURL` — رابط استلام الطلبات.

## ✅ تحسينات وإضافات مقترحة
- اختبار نشر Apps Script والتحقق من الاستجابة JSON عند الإرسال.
- إضافة رسائل نجاح/فشل أكثر تفصيلاً بدلاً من `alert` في حالة الخطأ.
- اختبارات واجهة (viewport) تلقائية أو مقاسات مخصصة لتأكيد أن `.hero-title` لا تنكسر على جميع أجهزة الهاتف شائعة الاستخدام.

## 📄 ملف واحد في المشروع
- `قالب_صفحة_هبوط_لبيع_الأحذية.html` — كل HTML/CSS/JS مضمنة في ملف واحد.

## 📬 تواصل / مساهمة
أخبرني إن أردت:
- ترجمة الملف كاملاً إلى الإنجليزية
- تحسين تجربة النماذج (أمثلة رسائل الخطأ، نماذج اختبار)
- مساعدة في نشر واختبار Google Apps Script

---
*README مختصر ومباشر ليُسهل عليك الفهم، الاختبار، والنشر.*
